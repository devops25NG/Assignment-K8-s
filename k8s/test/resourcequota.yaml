apiVersion: v1
kind: ResourceQuota
metadata:
  name: equal-quota
  namespace: test
spec:
  hard:
    requests.cpu: "2"         # IDENTICAL to dev
    requests.memory: "4Gi"    # IDENTICAL to dev
    limits.cpu: "2"
    limits.memory: "4Gi"
    pods: "15"
    services: "10"
