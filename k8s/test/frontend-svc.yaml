apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: test
spec:
  type: NodePort
  ports:
    - targetPort: 8080
      port: 8080
      nodePort: 30145
  selector:
    module: frontend     